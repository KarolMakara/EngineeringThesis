---
- name: Setup Calico on k3s
  hosts: "{{ target | default('localhost') }}"
  vars_files:
    - ./vars/common.yml

  roles:
    - delete_calico
    - delete_cilium
    - delete_antrea
    - uninstall_k3s
    - delete_cni
    - install_k3s
    - setup_k3s_antrea
